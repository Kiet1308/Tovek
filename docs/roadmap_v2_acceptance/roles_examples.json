[
 {
  "case": "role_results",
  "opt": 0,
  "debug": 1,
  "before_exact_names": 1,
  "after_exact_names": 1,
  "before": "local function measures(p)\n\treturn p.Width, p.Height\nend\n\nreturn function(p, p2)\n\tlocal v, v2 = measures(p)\n\tlocal v3, v4 = measures(p2)\n\treturn v, v2, v3, v4\nend\n",
  "after": "local function measures(p)\n\treturn p.Width, p.Height\nend\n\nreturn function(p, p2)\n\tlocal width, height = measures(p)\n\tlocal width2, height2 = measures(p2)\n\treturn width, height, width2, height2\nend\n"
 },
 {
  "case": "role_results",
  "opt": 1,
  "debug": 1,
  "before_exact_names": 1,
  "after_exact_names": 1,
  "before": "local function measures(p)\n\treturn p.Width, p.Height\nend\n\nreturn function(p, p2)\n\tlocal v, v2 = measures(p)\n\tlocal v3, v4 = measures(p2)\n\treturn v, v2, v3, v4\nend\n",
  "after": "local function measures(p)\n\treturn p.Width, p.Height\nend\n\nreturn function(p, p2)\n\tlocal width, height = measures(p)\n\tlocal width2, height2 = measures(p2)\n\treturn width, height, width2, height2\nend\n"
 },
 {
  "case": "buffer_role_slots",
  "opt": 0,
  "debug": 1,
  "before_exact_names": 1,
  "after_exact_names": 7,
  "before": "return function(buf, offset, value, p, p2, p3, p4, p5)\n\tbuffer.writebits(buf, offset, value, p)\n\tbuffer.copy(buf, p3, p2, p4, p5)\n\treturn buffer.readbits(buf, offset, value), buffer.tostring(buf)\nend\n",
  "after": "return function(p, bitOffset, bitCount, value, source, targetOffset, sourceOffset, count)\n\tbuffer.writebits(p, bitOffset, bitCount, value)\n\tbuffer.copy(p, targetOffset, source, sourceOffset, count)\n\treturn buffer.readbits(p, bitOffset, bitCount), buffer.tostring(p)\nend\n"
 },
 {
  "case": "buffer_role_slots",
  "opt": 1,
  "debug": 1,
  "before_exact_names": 1,
  "after_exact_names": 7,
  "before": "return function(buf, offset, value, p, p2, p3, p4, p5)\n\tbuffer.writebits(buf, offset, value, p)\n\tbuffer.copy(buf, p3, p2, p4, p5)\n\treturn buffer.readbits(buf, offset, value), buffer.tostring(buf)\nend\n",
  "after": "return function(p, bitOffset, bitCount, value, source, targetOffset, sourceOffset, count)\n\tbuffer.writebits(p, bitOffset, bitCount, value)\n\tbuffer.copy(p, targetOffset, source, sourceOffset, count)\n\treturn buffer.readbits(p, bitOffset, bitCount), buffer.tostring(p)\nend\n"
 },
 {
  "case": "buffer_role_slots",
  "opt": 2,
  "debug": 1,
  "before_exact_names": 1,
  "after_exact_names": 7,
  "before": "return function(buf, offset, value, p, p2, p3, p4, p5)\n\tbuffer.writebits(buf, offset, value, p)\n\tbuffer.copy(buf, p3, p2, p4, p5)\n\treturn buffer.readbits(buf, offset, value), buffer.tostring(buf)\nend\n",
  "after": "return function(p, bitOffset, bitCount, value, source, targetOffset, sourceOffset, count)\n\tbuffer.writebits(p, bitOffset, bitCount, value)\n\tbuffer.copy(p, targetOffset, source, sourceOffset, count)\n\treturn buffer.readbits(p, bitOffset, bitCount), buffer.tostring(p)\nend\n"
 }
]
